export interface EventParameters{
    insuredId: string,
    scheduleId: number,
    countryISO: string
}
export interface DatabaseCredentials {
    host: string;
    user: string;
    password: string;
}